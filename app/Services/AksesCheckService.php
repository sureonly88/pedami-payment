<?php

namespace App\Services;

use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\Auth;

class AksesCheckService
{

	public function checkMenuAkses($url){

		// if (strpos($url,'roles')){
  //           if (!Auth::user()->hasPermissionTo('Setup Roles')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'managepdambjm')){
  //           if (!Auth::user()->hasPermissionTo('Manage Transaksi')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'man_transaksi_pln')){
  //           if (!Auth::user()->hasPermissionTo('Transaksi PLN Postpaid')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'man_pln_prepaid')){
  //           if (!Auth::user()->hasPermissionTo('Transaksi PLN Prepaid')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'man_nontaglis')){
  //           if (!Auth::user()->hasPermissionTo('Transaksi PLN Nontaglis')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'users')){
  //           if (!Auth::user()->hasPermissionTo('Konfigurasi User')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'lokets')){
  //           if (!Auth::user()->hasPermissionTo('Konfigurasi Loket')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'topups')){
  //           if (!Auth::user()->hasPermissionTo('Topup Saldo')) {
  //               return false;
  //           }
  //       }

  //       if (strpos($url,'register_hps')){
  //           if (!Auth::user()->hasPermissionTo('Konfigurasi Handphone')) {
  //               return false;
  //           }
  //       }

  //       return true;

	}
}